# Canonical GitHub repository setup

The canonical remote is `tbitcs/michigan-workforce-intelligence` and MUST remain private.
Repository metadata is declared in `.github/repository-metadata.yml` and enforced by the
external bootstrap package. Do not commit credentials, bootstrap authentication state, or
personal access tokens to this repository.
