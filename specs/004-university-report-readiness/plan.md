# Plan

Use existing IPEDS, crosswalk, ingestion and repository interfaces. Extend IPEDS major handling to avoid first/second-major collisions. Add bounded university CLI harvest/report commands; persist source artifacts and observations, keep read-only report generation separate. Validate official binaries before ingestion; run Docker CI and live MCP reads. Host RTK unavailable; use canonical container tooling.
