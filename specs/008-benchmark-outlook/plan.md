# Plan

Extend reviewed BLS series and public BEA/ACS comparisons with Florida and national benchmarks. Collect BLS state U-1 through U-6 and HUD public homelessness context with explicit vintages and limitations. Reuse stored artifacts where possible. Forecast only compatible stored series with adequate consecutive history; evaluate simple persistence/seasonal baselines on held-out observations and show error-based scenario bands, not causal certainty. Each trend receives a forecast or a stated reason for insufficient evidence.

Keep the confidential exchange separate from aggregate MCP/report evidence. Build three reviewed PDFs and a public evidence package, finish local and release checks, delete only the explicitly identified previous report release, then publish its replacement. Rewrite current user-facing guides without historical addenda; retain historical specs and evidence provenance as records rather than fabricate a new origin.

Institution status is independent of data coverage. Preserve existing outcome ingestion scope; expose a separate official-source candidate catalog. Public publication follows offline full-history credential scanning and release inventory; use an all-branch restrict-update ruleset with sole personal-repository administrator bypass.
